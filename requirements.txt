##############################################
# 🚀 AI VIDEO AUTOMATION SUITE - PRODUCTION
##############################################

# --- Core Web Framework & Worker ---
Flask==3.0.2
gunicorn==22.0.0
gevent==24.2.1
python-dotenv==1.0.1
redis==5.0.3
celery[redis]==5.4.0

# --- Data Validation (CRITICAL FOR CODE 2) ---
# The optimized worker heavily uses Pydantic schemas.
pydantic>=2.6.1

# --- AI Clients & APIs ---
openai>=1.51.2
replicate>=0.31.0
cloudinary>=1.40.0

# [CRITICAL FIX] 
# Your logs failed because you had version 0.2.27.
# The code uses 'elevenlabs.client' which requires v1.0+.
elevenlabs>=1.0.0

# --- HTTP & Scraping ---
requests==2.32.3
# (Optional APIs if you use their SDKs, otherwise requests handles them)
scrapingbee==2.0.1
google-search-results==2.4.2

# --- Resilience & Utilities ---
tenacity==8.3.0
python-slugify==8.0.4

# --- Media Processing ---
# Pillow is used for Thumbnail generation logic
Pillow==10.4.0

# Note: FFmpeg is a system binary. 
# It is not installed via pip. It is installed via Railway Nixpacks.
