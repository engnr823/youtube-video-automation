##############################################
# 🚀 AI VIDEO EDITOR & AUTOMATION SUITE
##############################################

# --- Core Web Framework & Worker ---
Flask==3.0.2
gunicorn==22.0.0
gevent==24.2.1
python-dotenv==1.0.1
redis==5.0.3
celery[redis]==5.4.0

# --- Data Validation ---
pydantic>=2.6.1

# --- AI Clients (Whisper, GPT-4, Flux) ---
openai>=1.51.2
replicate>=0.31.0
cloudinary>=1.40.0

# --- Video & Audio Editing (NEW FOR SYSTEM 2) ---
# Required for silence removal, audio manipulation, and subtitle logic
moviepy>=1.0.3
pydub>=0.25.1
srt>=3.5.3

# --- HTTP & Utilities ---
requests==2.32.3
tenacity==8.3.0
python-slugify==8.0.4

# --- Media Processing ---
Pillow==10.4.0

# --- Legacy / Generator Support (Optional) ---
# Keep these if you still want the option to run the 'Generator' system
elevenlabs>=1.0.0
scrapingbee==2.0.1
google-search-results==2.4.2

# Note: FFmpeg is NOT installed via pip. 
# It must be installed on the server (Railway/Docker) via Nixpacks or Apt.
