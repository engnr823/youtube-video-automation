##############################################
# 🚀 AI VIDEO EDITOR & AUTOMATION SUITE
##############################################

# --- Core Web Framework & Worker ---
Flask==3.0.2
gunicorn==22.0.0
gevent==24.2.1
python-dotenv==1.0.1
redis==5.0.3
celery[redis]==5.4.0

# --- Data Validation ---
pydantic>=2.6.1

# --- AI Clients (Whisper, GPT-4, Flux) ---
openai>=1.51.2
replicate>=0.31.0
cloudinary>=1.40.0

# --- Video & Audio Editing (CRITICAL FOR EDITOR) ---
# moviepy: Used for complex edits if FFmpeg fails
# pydub: Used for audio manipulation (silence detection support)
# srt: Used for parsing subtitle timestamps safely
moviepy>=1.0.3
pydub>=0.25.1
srt>=3.5.3
imageio>=2.34.0
imageio-ffmpeg>=0.4.9

# --- HTTP & Utilities ---
requests==2.32.3
tenacity==8.3.0
python-slugify==8.0.4

# --- Media Processing ---
Pillow==10.4.0

# --- Legacy / Generator Support (Optional) ---
# You can remove these if you are ONLY running the Editor now, 
# but keeping them doesn't hurt.
elevenlabs>=1.0.0
scrapingbee==2.0.1
google-search-results==2.4.2
